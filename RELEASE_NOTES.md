# v0.6.0 Android 多引擎测试版

- Android 首次打开会询问 LiteRT-LM 智能 GPU/CPU 回退或 CPU 兼容模式。
- 设置区域支持随时切换引擎，并显示实际使用的 GPU、CPU 或回退状态。
- 加入 Qwen 3.5 4B、Gemma 4 E2B/E4B、Gemma 3n E2B/E4B 五个可安装视觉模型；已安装则跳过下载。
- 每个模型都有 Android 版本、内存与设备建议；下载固定版本并校验大小和 SHA-256。
- 识别输入最长边缩至 1280 像素，降低发热、内存和批量处理时间。
- 只有模型报告实质疑点时才启动视觉校对；未通过会自动修正并复核。
- 照片卡片改用序号，编辑框默认隐藏，并显示格式、尺寸和拍摄时间。
- 加入选择文件夹的批量导出按钮，以及随进度升调、50% 和 100% 区分的声音反馈。
- 同步修复 Lightroom 嵌套 XMP 场景：直接字段插入最外层 rdf:Description，不破坏既有 XMP。

# v0.4.0 测试版

本版为 macOS 和 Android 同步加入工作区删除与历史保留功能。

- 新增默认折叠的历史区域。
- 上一次会话中已有描述的照片在下次启动时自动进入历史，当前工作区从空白开始。
- 历史批次默认使用“年月日 时分秒”命名。
- 历史默认保留 30 天，可调整保留期；过期记录会在软件启动或保留期改变时自动清理。
- 支持删除单条记录、整个历史批次和全部历史。
- 支持清空当前工作区；有描述的项目进入历史。
- macOS 支持 Command-退格删除当前所选素材，Android 外接键盘支持 Meta/Command-退格。
- 删除当前项目会同时移除对应识别结果。
- 所有删除、历史清理和自动过期操作只影响软件记录，不删除磁盘或手机媒体库中的原始照片及导出文件。

Android 使用 LiteRT-LM 0.15.0 在本地运行 Qwen3.5 4B。当前 Android 测试版继续仅对 JPEG、PNG 启用可验证的原文件描述写入。

当前为测试签名版本。macOS 需要 Apple 芯片和 macOS 14 或更高版本；Android 需要 Android 8.0 或更高版本的 64 位 ARM 设备。
