# v0.3.0 macOS 测试版

本版重点修复批量浏览、人工编辑、导出和无障碍描述实际写入问题。

- 左侧保持完整原始素材列表、选择状态和处理状态。
- 右侧一次列出全部正在处理和已识别照片，可使用上下方向键浏览。
- 描述默认以只读文字显示，编辑框隐藏；点击每项右侧“编辑”才展开。
- 新增“批量导出已选结果”，由用户选择目标文件夹；默认保留原图，重名时自动生成安全的新文件名。
- 每个导出副本都会注入直接、扁平的 Iptc4xmpExt:ArtworkContentDescription 原始 XMP packet，并进行写入内验证和独立复核。
- 只有字段逐字回读一致、直接字段存在、错误嵌套字段不存在、文件名和尺寸不变、解码像素签名不变时才显示为完成。
- 新增本地模型校对轮次；未通过时将具体问题自动返回视觉模型重写，未经校对的描述不显示。
- 识别时每秒播放低音量进度提示，音调逐渐升高；50% 使用双音提示，100% 使用完成音，可在设置中关闭。

系统要求：Apple 芯片 Mac、macOS 14 或更高版本。安装包不内置数 GB 模型；缺少环境时仍会先征得用户同意，再按需下载和配置。

当前为未公证的测试签名版本。若系统阻止首次打开，请在 Finder 中按住 Control 点按应用并选择“打开”。
