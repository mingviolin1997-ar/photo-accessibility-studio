# v0.2.0 测试版

这是供朋友测试的首个 macOS 与 Android 双平台版本。两个安装包都不内置大模型，首次打开时会先检查本机环境；只有用户确认后，才会下载并自动配置所需组件和 Qwen3.5 4B 模型。下载界面会显示进度、已下载容量、总容量和实时速度。

## macOS

- 支持 Apple 芯片、macOS 14 或更高版本。
- 首次使用可按需安装私有 Ollama、ExifTool 和 Qwen3.5 4B；已有可用环境时会直接复用。
- 当前为未公证的测试签名版本。若系统阻止首次打开，请在 Finder 中按住 Control 点按应用并选择“打开”。

## Android

- 使用 LiteRT-LM 0.15.0 在设备本地运行多模态 Qwen3.5 4B。
- 支持 Android 8.0 或更高版本、arm64-v8a 设备。
- 模型约 5.26 GB，建议使用 Wi-Fi，并预留至少 6 GB 可用空间；支持断点续传和 SHA-256 完整性验证。
- 当前测试版仅对 JPEG、PNG 照片启用安全的原文件描述写入；其他格式会明确报错，不会伪装成功。
- APK 使用测试证书签名，需要允许从浏览器或文件管理器安装未知应用。

两个版本都会在本地处理照片，不上传照片；写入后自动验证直接、扁平的 Apple Preview/iPhone 可读 XMP 图片描述字段，并检查文件名、尺寸和解码后像素未改变。
