# v0.6.0 macOS MLX 双引擎测试版

本版把 Mac 默认推荐推理路径升级为 MLX，同时完整保留 Ollama 回退和原有 XMP 写入流程。

- 首次检测到 Mac 且尚未选择引擎时，先询问使用 MLX 或 Ollama；MLX 标为推荐。
- 设置新增推理引擎区域，可查看说明并在 MLX/Ollama 之间切换；缺少环境时仍先询问再自动配置。
- MLX 使用独立的 Python 3.12 + MLX-VLM 0.6.6 环境，不修改系统 Python；使用 uv 0.11.10 自动准备，检测到本机 uv 时直接复用。
- Qwen 默认使用约 3.06 GB 的 `mlx-community/Qwen3.5-4B-MLX-4bit`。模型下载显示百分比、已下载/总大小和实时速度，并由 Hugging Face 校验文件。
- Gemma 4 E2B/E4B 与 Gemma 3n E2B/E4B 均增加 MLX 视觉模型映射；Gemma 3n 在 MLX 可识图，在 Ollama 仍避免误选文本包。
- 用户 10 张 HEIC/JPEG/16 位 PNG 样片在 MLX 中识别 10/10 成功，总计约 20.34 秒；同机 Ollama 基线约 30.13 秒。
- 真实端到端测试覆盖 MLX 环境安装、模型下载、照片识别、直接扁平 XMP 写入、精确回读和像素不变验证。
- 第二次 MLX 端到端测试直接复用已安装环境与模型，3.76 秒完成，没有重复下载。

以下 v0.5.0 的性能、格式、历史记录、键盘操作和界面改进全部保留。

## v0.5.0 基线

本版重点修复批量识别高负载、随机格式错误以及部分 Lightroom JPEG 无法写入描述的问题。

- 用 Ollama JSON Schema 取代容易随机失败的普通 JSON 模式。
- 默认改为单次生成内自检，只在发现疑点时进行独立视觉校对；可在设置中恢复每张严格复核。
- 图像预处理改用后台 ImageIO，最长边降至 1280，降低主界面卡顿、内存和视觉 token 消耗。
- 用户提供的 10 张 HEIC/JPEG/16 位 PNG 样本识别 10/10 成功，总计约 30.7 秒。
- 10 张样本全部通过直接扁平 XMP 写入、精确回读和像素不变测试。
- 修正 Lightroom XMP 内嵌 rdf:Description 导致直接字段写入错误层级的问题。
- 删除重复的全尺寸像素解码和重复 ExifTool 验证，保留一次最终权威完整验证。
- 结果改用第 1、2、3 张编号，不显示乱码文件名；详细信息新增拍摄时间、格式和尺寸。
- Command-逗号现在打开标准设置窗口。
- 设置新增 Gemma 4 E2B、Gemma 4 E4B、Gemma 3n E2B、Gemma 3n E4B 下载目录、安装检测和设备建议。
- 点击模型会先检查本机；已安装则跳过下载，缺少时才显示进度、实时速度并自动配置。

历史记录、30 天保留、Command-退格、按需编辑、批量导出、直接扁平 XMP 和进度声音均继续保留。

当前为测试签名版本，需要 Apple 芯片和 macOS 14 或更高版本。
